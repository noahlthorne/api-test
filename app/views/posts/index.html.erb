<div class="container">

<div class="heading">
  <h1>Tweets</h1>
</div>
<div class="new-post btn draw-border">
  <%= render "create_card" %>
</div>

  <div class="posts">
      <% @posts.each do |post| %>
        <%= render 'post_card', post: post %>
      <% end %>
  </div>

</div>

